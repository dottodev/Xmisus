plugins {
    id 'com.android.application'
}

android {
    namespace 'com.shadow.mlbbcheat'
    compileSdk 34

    defaultConfig {
        applicationId "com.shadow.mlbbcheat"
        minSdk 24
        targetSdk 34
        versionCode 2
        versionName "1.1.0"
    }

    signingConfigs {
        release {
            storeFile file('C:/Users/mitra/AppData/Local/Temp/opencode/mlbb-build/mlbbcheat.jks')
            storePassword 'mlbbcheat123'
            keyAlias 'mlbbcheat'
            keyPassword 'mlbbcheat123'
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            signingConfig signingConfigs.release
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    testOptions {
        unitTests {
            includeAndroidResources = true
        }
    }
}

buildDir = file('C:/Users/mitra/AppData/Local/Temp/opencode/mlbb-build/app')

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'com.google.android.gms:play-services-ads:22.6.0'
    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.robolectric:robolectric:4.11.1'
}
